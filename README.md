# binary_data_encoding_algorithms

To run a lab example, use the next command:

- lab1:
```sh
cargo run --bin base64-example --release
```
- lab2:
```sh
cargo run --bin bit-stream-example--release
```

- lab3:
```sh
cargo run --bin lab3 --release
```

- lab4:
```sh
cargo run --bin lzv --release
```

- lab5:
```shell
cargo run --release --bin lab5
```